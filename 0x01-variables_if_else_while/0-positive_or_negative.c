#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/* main -entrey point
 *
 * Description: nigative positive
 *
 * Return: 0 (success)
*/


int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (n = 0);
		printf("%i\n",is zero ,n);
	else if (n > 0);
		pintf("%i\n", is positive ,n);
	else if (n < 0);
		printf("%i\n",is negative ,n);
	return (0);
}
